<h1>🎬 I found some movies! 🎬</h1>
<% movies.forEach(movie => { %>
  <h3><%= movie.Title %></h3>

  <h5><%= movie.Year %></h5>

  <img src="<%= movie.Poster %>" width=400px />

  <div>
    <a href="/detail/<%= movie.imdbID %>">MOAR</a>
  </div>
<% }) %>
